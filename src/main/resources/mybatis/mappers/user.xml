<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">
	
	<!-- 김준호 -->
	<select id="login" parameterType="com.lunchwb.vo.UserVo" resultType="com.lunchwb.vo.UserVo">
		<![CDATA[
			select user_no userNo
			        ,user_email userEmail
			        ,user_name userName
			        ,user_grade userGrade
			from users
			where user_email = #{userEmail}
			and user_password = #{userPassword} 		
		]]>
	</select>
	
	<insert id="join" parameterType="com.lunchwb.vo.UserVo">
		<![CDATA[
			insert into users
			values (seq_user_no.nextval, #{userEmail}, #{userPassword}, #{userName}, #{userBirth}, #{userSex}, sysdate, 0)
		]]>
	</insert>
	
	<select id="getUser" parameterType="com.lunchwb.vo.UserVo" resultType="com.lunchwb.vo.UserVo">
		<![CDATA[
			select user_email userEmail
			        ,user_name userName
			        ,to_char(user_Birth, 'yyyy-mm-dd') userBirth
			        ,user_sex userSex
			from users
			where user_email = #{userEmail}
			and user_password = #{userPassword} 		
		]]>
	</select>
	
	<update id="modifyUser" parameterType="com.lunchwb.vo.UserVo">
		<![CDATA[
			update users
			set user_password = #{userPassword}
			    ,user_name = #{userName}
			    ,user_birth = #{userBirth}
			    ,user_sex = #{userSex}
			where user_email = #{userEmail}
		]]>
	</update>
	
	
	
	<!-- 이지희 -->
	   <select id="groupMemInfo" parameterType="int" resultType="com.lunchwb.vo.UserVo">
	  		 select  user_no userNo,
			         user_name userName,
			         to_char(user_birth, 'YYYYMMDD') userBirth,
			         user_sex userSex
	         from users
	         where user_no = #{userNo}
	   </select>

</mapper>